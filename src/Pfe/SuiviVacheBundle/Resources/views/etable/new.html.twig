{% extends 'layout_admin.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block body %}
    <div class="row marg_bot_20">
        <div class="col-md-12">
            <ul class="liste_btn_action">
                <li>
                    <a class="btn btn-info" href="{{ path('etable_index') }}"><span class="fa fa-bars"></span> Liste des étables</a>
                </li>
            </ul>
        </div>
    </div>
    <h1>Etable creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    <div class="row">
        <div class="col-md-12">
            <input type="submit" class="btn btn-success" value="Enregistrer" />
        </div>
    </div>
    {{ form_end(form) }}


{% endblock %}
