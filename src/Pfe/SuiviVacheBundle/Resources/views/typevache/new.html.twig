{% extends 'layout_admin.html.twig' %}

{% block body %}
    <h2>Ajouter un type de vache</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('typevache_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
