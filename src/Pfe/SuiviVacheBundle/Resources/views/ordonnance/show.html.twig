{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ordonnance</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordonnance.id }}</td>
            </tr>
            <tr>
                <th>Dateordonnance</th>
                <td>{% if ordonnance.dateOrdonnance %}{{ ordonnance.dateOrdonnance|date('Y-m-d H:i:s') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ordonnance_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ordonnance_edit', { 'id': ordonnance.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
