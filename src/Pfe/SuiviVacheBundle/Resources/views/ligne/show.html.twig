{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ligne</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne.id }}</td>
            </tr>
            <tr>
                <th>Qterecu</th>
                <td>{{ ligne.qterecu }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ligne_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ligne_edit', { 'id': ligne.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
